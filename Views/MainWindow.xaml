<Window x:Class="LuceneSearchWPFApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LuceneSearchWPFApp"
        xmlns:viewmodels="clr-namespace:LuceneSearchWPFApp.ViewModels"
        xmlns:utilities="clr-namespace:LuceneSearchWPFApp.Utilities"
        mc:Ignorable="d"
        Title="LuceneSearch WPF" Height="700" Width="1024">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <utilities:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Control Panel -->
        <Border Grid.Row="0" Padding="10" Background="#f0f0f0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: 資料來源設定 (Date, Path, Filter) -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                         <RowDefinition Height="Auto"/>
                         <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Date Range Selection (Moved to Top) -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="日期範圍:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="Bold"/>
                        
                        <DatePicker SelectedDate="{Binding StartDate}" Width="120" Height="27" VerticalAlignment="Center"/>
                        <TextBlock Text="~" VerticalAlignment="Center" Margin="5,0,5,0"/>
                        <DatePicker SelectedDate="{Binding EndDate}" Width="120" Height="27" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        
                        <!-- 快速選擇 -->
                        <Button Content="最近 3 天" Width="70" Height="25" Margin="0,0,5,0" Command="{Binding SetDateRangeCommand}" CommandParameter="3" Background="#E3F2FD" BorderBrush="#2196F3" Foreground="#1976D2"/>
                        <Button Content="最近 7 天" Width="70" Height="25" Margin="0,0,5,0" Command="{Binding SetDateRangeCommand}" CommandParameter="7" Background="#E3F2FD" BorderBrush="#2196F3" Foreground="#1976D2"/>
                        <Button Content="最近 30 天" Width="70" Height="25" Margin="0,0,5,0" Command="{Binding SetDateRangeCommand}" CommandParameter="30" Background="#E3F2FD" BorderBrush="#2196F3" Foreground="#1976D2"/>
                    </StackPanel>

                    <!-- Path & Filter (Moved to Second Row) -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="0,0,0,5">
                         <TextBlock Text="Path" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="Bold"/>
                         <TextBox Text="{Binding FolderPath, UpdateSourceTrigger=PropertyChanged}" Width="300" Height="27" VerticalAlignment="Center" Margin="0,0,10,0"/>

                         <TextBlock Text="Filter" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="Bold"/>
                         <ComboBox x:Name="cmbFilter" Width="150" Height="27" VerticalAlignment="Center" Margin="0,0,10,0"
                                  IsEditable="True" IsTextSearchEnabled="True"
                                  ItemsSource="{Binding FileFilterOptions}"
                                  SelectedItem="{Binding FileFilterKeyword, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>

                <!-- Row 1: 操作區 (建立索引) -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                    <Button Content="建立索引" Width="120" Height="35" Command="{Binding CreateIndexCommand}" IsEnabled="{Binding IsIndexing, Converter={StaticResource InverseBooleanConverter}}" FontWeight="Bold" Background="#2196F3" Foreground="White"/>
                    <Button Content="清空索引" Width="100" Height="35" Margin="10,0,0,0" Command="{Binding ClearIndexCommand}" IsEnabled="{Binding IsIndexing, Converter={StaticResource InverseBooleanConverter}}" Background="#F44336" Foreground="White" FontWeight="Bold"/>
                    <Button Content="刷新 Filter 清單" Width="120" Height="35" Margin="10,0,0,0" Command="{Binding RefreshFiltersCommand}"/>
                    
                    <TextBlock Text="{Binding IndexProgressMessage}" VerticalAlignment="Center" Margin="15,0,0,0" Foreground="#666" TextTrimming="CharacterEllipsis" MaxWidth="500"/>
                </StackPanel>

                <!-- Row 2: 搜尋區 -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="搜尋關鍵字:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="Bold"/>
                    <TextBox Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}" Width="300" Height="27" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="搜尋" Width="100" Height="35" Command="{Binding SearchCommand}" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
                </StackPanel>

                <!-- Row 3: 顯示選項 & 狀態 -->
                <StackPanel Grid.Row="3" Orientation="Horizontal">
                    <CheckBox IsChecked="{Binding GroupByDate}" Content="依日期分組顯示" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ResultCountText}" VerticalAlignment="Center" Margin="20,0,0,0" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Result Area -->
        <DataGrid Grid.Row="1" Margin="10"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5"
                  RowHeight="30"
                  FontFamily="Microsoft JhengHei UI"
                  FontSize="12"
                  ItemsSource="{Binding SearchResults}"
                  SelectedItem="{Binding SelectedResult}"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="日期" Binding="{Binding FileDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="時間戳記" Binding="{Binding LogTimestamp}" Width="140"/>
                <DataGridTextColumn Header="檔案名稱" Binding="{Binding FileName}" Width="200"/>
                <DataGridTextColumn Header="行號" Binding="{Binding LineNumber}" Width="60"/>
                <DataGridTemplateColumn Header="內容" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Content}" />
                            <!-- 高亮邏輯將需要 View 層的附加屬性或 DataGridTemplateColumn 中的更複雜的 TextBlock 設置 -->
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="📂 用預設編輯器開啟" Command="{Binding OpenLogEntryCommand}"/>
                    <MenuItem Header="📋 複製此行內容" Command="{Binding CopyContentCommand}"/>
                    <MenuItem Header="📋 複製檔案路徑" Command="{Binding CopyFilePathCommand}"/>
                    <Separator/>
                    <MenuItem Header="📊 只顯示此檔案的結果" Command="{Binding FilterByFileCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</Window>
